<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.realworld.conduit.mapper.ArticleFavoriteMapper">

    <resultMap id="ArticleFavoriteResultMap" type="com.realworld.conduit.model.ArticleFavorite">
        <id property="id" column="id"/>
        <result property="articleId" column="article_id"/>
        <result property="userId" column="user_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insertFavorite" parameterType="com.realworld.conduit.model.ArticleFavorite" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO article_favorites (article_id, user_id, created_at)
        VALUES (#{articleId}, #{userId}, NOW())
    </insert>

    <delete id="deleteFavorite">
        DELETE FROM article_favorites 
        WHERE article_id = #{articleId} AND user_id = #{userId}
    </delete>

    <select id="isFavorited" resultType="boolean">
        SELECT COUNT(*) > 0 
        FROM article_favorites 
        WHERE article_id = #{articleId} AND user_id = #{userId}
    </select>

    <select id="countFavorites" resultType="int">
        SELECT COUNT(*) 
        FROM article_favorites 
        WHERE article_id = #{articleId}
    </select>

</mapper>